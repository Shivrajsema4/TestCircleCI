version: 2
jobs:
   build:
     docker:
       - image: circleci/java:8
     steps:
       - checkout
       - run: echo "hello world"
workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
         type: approval
         
